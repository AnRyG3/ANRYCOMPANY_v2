# Codex 重さ対策メモ 2026-06-04

## 今日わかった原因

- 画像生成をしていない時でも、Codex の通常確認でサンドボックス準備が失敗することがある。
- 主因は `F:\ANRYCAMPANY` 全体を大きな作業場所として扱っていること。
- Git 状態がかなり重い。特に削除扱いの追跡済みファイルが多い。
  - `D`: 604 件
  - `??`: 102 件
  - `M`: 17 件
  - `A`: 44 件
- 削除扱いの大半は `02_LINEスタンプ` 配下の古い制作素材。

## 今日実施した対策

- `.gitignore` に生成物・中間素材・分析出力・動画画像素材を追加。
- Git の未追跡ファイル確認を軽くする設定 `core.untrackedCache=true` を有効化。
- 今後の制作で、提出物以外の中間画像がGit状態に出にくいようにした。

## まだ残っている根本原因

`.gitignore` は「これから追加される未追跡ファイル」には効くが、すでにGitが覚えているファイルの削除状態には効かない。

そのため、604件の削除扱いは別途整理が必要。

## 次にやるべきこと

1. `02_LINEスタンプ` の古い試作・中間素材の削除をGit上でも確定する。
2. 必要な完成品だけを残す。
3. 可能なら、今後Codexで開く作業場所は `F:\ANRYCAMPANY` 全体ではなく、対象プロジェクト単位にする。
4. LINEスタンプ制作は8枚ずつ確認し、最終提出フォルダだけを残す。

## 判断が必要なこと

604件の削除扱いをGit上でも確定してよいか。

これは実ファイルを追加で消す操作ではなく、「すでに消えている・移動済みの古い素材を、Gitの記録上も整理する」操作。
